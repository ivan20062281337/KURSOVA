<Window x:Class="LIBRARY.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LIBRARY"
        mc:Ignorable="d"
        Title="Панель керування бібліотекою" Height="500" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Книги та Автори">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0">
                        <Button Content="Додати книгу" Click="AddBook_Click" Width="110" Height="30" Margin="10,0,10,0"/>
                        <Button Content="Додати автора" Click="AddAuthor_Click" Width="110" Height="30" Margin="0,0,10,0"/>
                        <Button Content="Додати категорію" Click="AddCategory_Click" Width="110" Height="30" Margin="0,0,10,0"/>
                        <Button Content="Оновити таблиці" Click="Refresh_Click" Width="110" Height="30"/>
                    </StackPanel>

                    <DataGrid x:Name="dgBooks" AutoGenerateColumns="False" IsReadOnly="True" Grid.Row="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding book_id}" Width="40"/>
                            <DataGridTextColumn Header="Назва" Binding="{Binding title}" Width="*"/>
                            <DataGridTextColumn Header="ISBN" Binding="{Binding isbn}" Width="100"/>
                            <DataGridTextColumn Header="Автор" Binding="{Binding author_name}" Width="*"/>
                            <DataGridTextColumn Header="Категорія" Binding="{Binding category_name}" Width="100"/>
                            <DataGridTextColumn Header="К-сть" Binding="{Binding quantity}" Width="50"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Студенти">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="Додати студента" Grid.Column="0"
                        HorizontalAlignment="Left" Margin="10" Padding="10,5"
                        Click="AddStudent_Click" Background="#4CAF50" Foreground="White"/>

                    <DataGrid x:Name="dgStudents" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,5,0,5" Grid.Row="1" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding student_id}" Width="40"/>
                            <DataGridTextColumn Header="ПІБ" Binding="{Binding full_name}" Width="*"/>

                            <DataGridTemplateColumn Header="Дії" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Взаємодія" 
                                        Click="OpenInteraction_Click" 
                                        Padding="5,2" 
                                        Background="#2196F3" 
                                        Foreground="White" 
                                        FontWeight="Bold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
