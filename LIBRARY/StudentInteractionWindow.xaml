<Window x:Class="LIBRARY.StudentInteractionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LIBRARY"
        mc:Ignorable="d"
        Title="Взаємодія зі студентом" Height="600" Width="500" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblStudentName" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Header="Книги на руках (Борги)" Grid.Row="1" Margin="0,0,0,10">
            <DataGrid x:Name="dgActiveLoans" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Назва книги" Binding="{Binding book_title}" Width="*"/>
                    <DataGridTextColumn Header="Додати до" Binding="{Binding return_date, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTemplateColumn Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Повернути" Click="Return_Click" Tag="{Binding loan_id}" Background="LightCoral"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Header="Видати нову книгу" Grid.Row="2">
            <DataGrid x:Name="dgAvailableBooks" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Назва" Binding="{Binding title}" Width="*"/>
                    <DataGridTextColumn Header="К-сть" Binding="{Binding quantity}" Width="50"/>
                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Видати" Click="Issue_Click" Tag="{Binding book_id}" Background="LightGreen"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
